export const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;
export const PHONE_RE = /^\+?[0-9][0-9\s\-]{6,14}$/; // 7–15 digits, optional leading +
export const ISBN_RE = /^[0-9][0-9\s\-]*[0-9Xx]$/; // digits with optional spaces / hyphens
